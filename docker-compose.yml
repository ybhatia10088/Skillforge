services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: skillforge
      POSTGRES_PASSWORD: skillforge
      POSTGRES_DB: skillforge
    ports:
      - "5432:5432"
    volumes:
      - skillforge_pg_data:/var/lib/postgresql/data

volumes:
  skillforge_pg_data:
